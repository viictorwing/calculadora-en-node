{
  "name": "app",
  "version": "0.0.1",
  "description": "Es una tabla de multiplicar que también crea un archivo con la tabla.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Victor Sanchez",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "yargs": "^16.2.0"
  }
}
